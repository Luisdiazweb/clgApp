Central Logistics hardware store app.

- Run `npm install` to install dependencies
- Run `ionic serve` to serve
- Run `ionic run android` to run on adroid device
- Run `ionic build android` to debug build apk